import { Register } from '../../form';

const RegSection = () => {
    return <section className="w-screen h-screen flex items-center justify-center bg-blue">
        <Register hideBtn={true} />
    </section>;
};

export default RegSection;
