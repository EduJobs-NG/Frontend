export const employer = ({close}) => {
    return <form className="w-full py-8" onClick={close}>employer</form>;
};

export const jobseeker = ({close}) => {
    return <form className="w-full py-8" onClick={close}>jobseeker</form>;
};